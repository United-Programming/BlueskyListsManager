Add a way to save preferences (atUris)
Store the expiration of the session, and auto-reconnect using some sort of timer
Change the UI stile, adding sub-section, that will show only the widgets we need, with a button on the bottom to perform the action

-------- DONE ------------------
!Add ability to copy starter pack users to a list
!Add "Get my lists"
!Add a way to check if user is inside list
!When merging lists allow to block users
!Add dropdown for lists
!Add "reload" button
!Add a way to add user to a list
!Replace layout with table
!Add a way to save preferences (user, password)
!Add login page
!Use the new way to get the session
!Move to the newer version of bsky 0.18.+